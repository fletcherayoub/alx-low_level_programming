#include "main.h"
/**
 *_memcpy - entry point
 *@dest: input
 *@src: input
 *@n: input
 *Return: Always 0 (success)
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int m;

	for (m = 0; i < n; i++)
		dest[m] = src[m];
	return (dest);
}
